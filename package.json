{
  "name": "preact-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch",
    "test": "eslint src && preact test",
    "heroku-postbuild": "yarn run build",
    "serve:heroku": "preact serve --server superstatic --port $PORT"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "devDependencies": {
    "eslint": "^4.1.1",
    "if-env": "^1.0.0"
  },
  "dependencies": {
    "autodll-webpack-plugin": "^0.0.6",
    "bootstrap": "^3.3.7",
    "font-awesome": "^4.7.0",
    "google-fonts-webpack-plugin": "^0.4.0",
    "halogen": "^0.2.0",
    "image-webpack-loader": "^3.3.1",
    "postcss": "^6.0.3",
    "preact": "^8.1.0",
    "preact-cli": "^1.3.0",
    "preact-compat": "^3.16.0",
    "preact-css-transition-group": "^1.3.0",
    "preact-router": "^2.5.2",
    "react-bootstrap": "^0.31.0",
    "react-transition-group": "^1.2.0",
    "react-typist": "^1.1.1",
    "react-waypoint": "^7.0.4",
    "superstatic": "^4.1.0"
  }
}
